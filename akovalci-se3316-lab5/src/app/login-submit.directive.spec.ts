import { LoginSubmitDirective } from './login-submit.directive';

describe('LoginSubmitDirective', () => {
  it('should create an instance', () => {
    const directive = new LoginSubmitDirective();
    expect(directive).toBeTruthy();
  });
});
